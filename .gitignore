# Build output (kernel objects, ELFs, disk image, host tools) — all regenerated by `make`
/build/

# Editor / OS cruft
*.o
*.swp
*~
.DS_Store

# Python bytecode cache (e.g. from importing tools/osdrive.py)
__pycache__/
*.pyc

# Playwright MCP scratch logs (created by research subagents' web fetching)
.playwright-mcp/
# the Firefox test URL: a real site to point the browser at, deliberately
# NOT part of the repository (M2241)
/ffurl.txt
